import {describe,expect,it,vi} from 'vitest';import {allocateHrtId,generateHrtId,HRT_PATTERN} from '@/lib/identity';
describe('anonymous HRT identity',()=>{it('uses nine non-ambiguous secure characters',()=>{for(let i=0;i<100;i++){const id=generateHrtId();expect(id).toMatch(HRT_PATTERN);expect(id).not.toMatch(/[OI01]/)}});it('retries collisions',async()=>{let calls=0;const exists=vi.fn(async()=>++calls<3);const id=await allocateHrtId(exists);expect(id).toMatch(HRT_PATTERN);expect(exists).toHaveBeenCalledTimes(3)});it('fails safely after repeated collisions',async()=>{await expect(allocateHrtId(async()=>true,2)).rejects.toThrow()})});
