import {describe,expect,it} from 'vitest';import {canTransition} from '@/lib/types';
describe('status transitions',()=>{it('allows contributor approval to moderation',()=>expect(canTransition('awaiting_user_approval','pending_moderation')).toBe(true));it('prevents contributors self-publishing',()=>expect(canTransition('pending_moderation','published')).toBe(false));it('allows moderator publication',()=>expect(canTransition('pending_moderation','published',true)).toBe(true));it('prevents drafts becoming public',()=>expect(canTransition('draft','published',true)).toBe(false))});
