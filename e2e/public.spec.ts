import {test,expect} from '@playwright/test';
test('homepage is keyboard navigable and carries the correct identity',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Why You Shouldn’t Meet My/})).toBeVisible();await page.keyboard.press('Tab');await expect(page.locator(':focus')).toBeVisible();await expect(page.getByText(/prefers-reduced-motion/)).toHaveCount(0)});test('browse exposes database empty or result state',async({page})=>{await page.goto('/browse');await expect(page.getByRole('heading',{name:'Browse published stories'})).toBeVisible()});
