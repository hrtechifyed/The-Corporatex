'use client';import {useState} from 'react';
export function ShareReport({id,title,reference}:{id:string;title:string;reference:string}){const [message,setMessage]=useState('');const url=typeof window==='undefined'?'':window.location.href;async function copy(value:string,label:string){await navigator.clipboard.writeText(value);setMessage(`${label} copied`)}async function report(){const reason=window.prompt('Briefly tell us why this story should be reviewed.');if(!reason)return;const r=await fetch('/api/reports',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({experienceId:id,reason:'community_concern',details:reason})});setMessage(r.ok?'Report received. Thank you.':'Report could not be sent.')}return <div className="mt-8 flex flex-wrap gap-3"><button className="btn btn-secondary" onClick={()=>copy(url,'Link')}>Copy link</button><button className="btn btn-secondary" onClick={()=>copy(reference,'Reference')}>Copy reference</button><a className="btn btn-secondary" rel="noopener noreferrer" target="_blank" href={`https://www.reddit.com/submit?url=${encodeURIComponent(url)}&title=${encodeURIComponent(title)}`}>Share on Reddit</a><a className="btn btn-secondary" rel="noopener noreferrer" target="_blank" href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`}>Share on LinkedIn</a><button className="btn btn-secondary" onClick={report}>Report story</button><span className="self-center text-sm" aria-live="polite">{message}</span></div>}
