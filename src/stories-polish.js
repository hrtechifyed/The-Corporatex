if (document.body?.dataset.refPage === 'stories') {
  const notice = document.querySelector('.directory-hero .notice');
  if (notice) notice.textContent = 'Genuine, moderated workplace stories appear here after contributor approval.';
}
