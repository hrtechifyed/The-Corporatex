import 'server-only';import {GoogleGenerativeAI} from '@google/generative-ai';import {analysisSchema} from './schemas';
const FIELDS=['suggestedHeadline','shortSummary','openingPromise','realityCheck','firstPlotTwist','recurringConflict','managementArc','leadershipArc','workloadAndBoundaries','growthAndPromotion','payAndBenefits','teamAndCulture','positiveMoments','finalTrigger','warningSigns','whoMayThrive','whoMayStruggle','candidateQuestions','wouldReturn','suggestedLabels','possibleIdentifyingDetails','possibleAbusiveContent','possibleUnsupportedClaims','seriousTopic'];
export async function analyseStory(source:{context:unknown;guided:unknown;freeText:string|null}){if(!process.env.GEMINI_API_KEY)throw new Error('GEMINI_API_KEY is not configured');const model=new GoogleGenerativeAI(process.env.GEMINI_API_KEY).getGenerativeModel({model:'gemini-1.5-flash',generationConfig:{responseMimeType:'application/json'}});const prompt=`You are a careful workplace-story editor, not a judge or fact checker. Organise only facts in SOURCE. Never invent, exaggerate, infer illegality, add names, or turn uncertainty into certainty. Preserve balanced positives. Flag possible identifying details, abusive language, unsupported claims, and serious topics. For serious harm use direct neutral language, never entertainment metaphors. Return one JSON object with exactly these camelCase fields: ${FIELDS.join(', ')}. warningSigns, candidateQuestions, suggestedLabels and all possible* fields are arrays of strings; seriousTopic is boolean; all others strings. Use empty strings/arrays where source has no support. SOURCE: ${JSON.stringify(source)}`;const result=await model.generateContent(prompt);return analysisSchema.parse(JSON.parse(result.response.text()))}
