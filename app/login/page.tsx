import {login} from './actions';
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;return <section className="mx-auto max-w-xl px-5 py-20"><p className="scene-tag">Private entrance</p><h1 className="mt-3 text-5xl font-black">Sign in by magic link</h1>{q.sent?<div role="status" className="cinema-card mt-8 p-6">Check your inbox. The link signs you in without a password.</div>:<form action={login} className="cinema-card mt-8 grid gap-5 p-7"><label className="label">Email address<input className="field" required type="email" name="email" autoComplete="email"/></label><input type="hidden" name="next" value={q.next||'/submit'}/><p className="text-sm text-white/60">Your email stays private and is never shown on a public story.</p>{q.error&&<p role="alert" className="text-red-300">{q.error}</p>}<button className="btn btn-primary">Email my magic link</button></form>}</section>}
