import {ownedExperience} from '@/lib/auth';import {GuidedEditor} from '@/components/guided-editor';
export default async function Guided({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await ownedExperience(id);const {data}=await supabase.from('guided_answers').select('question_key,answer').eq('experience_id',id).order('sort_order');return <section className="mx-auto max-w-7xl px-5 py-12"><p className="scene-tag">Guided Story · Private</p><h1 className="mt-3 text-5xl font-black">Choose the scenes that matter</h1><p className="prose-story mt-3">Skip any scene. Every saved answer is stored in your private Supabase draft so you can return later.</p><GuidedEditor id={id} saved={data||[]}/></section>}
