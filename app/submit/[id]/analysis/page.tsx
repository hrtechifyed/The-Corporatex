import {ownedExperience} from '@/lib/auth';import {AnalysisRunner} from '@/components/analysis-runner';
export default async function Analysis({params}:{params:Promise<{id:string}>}){const {id}=await params;const {experience}=await ownedExperience(id);return <section className="mx-auto max-w-3xl px-5 py-16"><p className="scene-tag">AI Story Editor · Private</p><h1 className="mt-3 text-5xl font-black">AI Story Editor</h1><p className="prose-story mt-4 text-lg"><b>We’ll organise your experience into a clearer story without changing what you meant. You get the final edit.</b></p><AnalysisRunner id={id} hasAnalysis={!!experience.ai_analysis}/></section>}
