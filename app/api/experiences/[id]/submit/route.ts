import {NextRequest,NextResponse} from 'next/server';import {ownedExperience} from '@/lib/auth';import {reviewSchema,uuidSchema} from '@/lib/schemas';import {HIGHLIGHT_FIELDS} from '@/lib/review';
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;uuidSchema.parse(id);const input=reviewSchema.parse(await req.json());const {supabase,experience}=await ownedExperience(id);if(experience.status!=='awaiting_user_approval')return NextResponse.json({error:'Only a reviewed analysis can be submitted'},{status:409});const rows=HIGHLIGHT_FIELDS.flatMap(([key,category],sort)=>{const content=String(input.analysis[key]||'').trim();return content?[{experience_id:id,category,content,contributor_approved:true,sort_order:sort}]:[]});await supabase.from('experience_highlights').delete().eq('experience_id',id);if(rows.length){const stored=await supabase.from('experience_highlights').insert(rows);if(stored.error)throw stored.error}await supabase.from('experience_labels').delete().eq('experience_id',id);if(input.labels.length)await supabase.from('experience_labels').insert(input.labels.map(label=>({experience_id:id,label})));const {error}=await supabase.from('experiences').update({ai_analysis:input.analysis,approved_headline:input.headline,approved_summary:input.summary,status:'pending_moderation'}).eq('id',id);if(error)throw error;return NextResponse.json({status:'pending_moderation'})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Submission failed'},{status:400})}}
