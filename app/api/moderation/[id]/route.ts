import {NextRequest,NextResponse} from 'next/server';import {requireModerator} from '@/lib/auth';import {uuidSchema} from '@/lib/schemas';import {slugify} from '@/lib/slug';import {canTransition,ExperienceStatus} from '@/lib/types';import {z} from 'zod';
const schema=z.object({action:z.enum(['edit','publish','reject','request_changes','unpublish']),privateReason:z.string().max(4000),headline:z.string().min(3).max(160),summary:z.string().min(20).max(1200)});const targets={publish:'published',reject:'rejected',request_changes:'changes_requested',unpublish:'withdrawn'} as const;
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;uuidSchema.parse(id);const input=schema.parse(await req.json());const {supabase,profile}=await requireModerator();const {data:experience,error}=await supabase.from('experiences').select('status,approved_headline,public_slug').eq('id',id).single();if(error)throw error;if(input.action==='edit'){const edited=await supabase.from('experiences').update({approved_headline:input.headline,approved_summary:input.summary}).eq('id',id);if(edited.error)throw edited.error;await supabase.from('moderation_actions').insert({experience_id:id,moderator_id:profile.id,action:'edit',private_reason:input.privateReason});return NextResponse.json({saved:true})}const target=targets[input.action];if(!canTransition(experience.status as ExperienceStatus,target,true))return NextResponse.json({error:'Invalid moderation status transition'},{status:409});let publicSlug=experience.public_slug;if(target==='published'&&!publicSlug)publicSlug=`${slugify(input.headline)}-${id.slice(0,8)}`;const patch:any={status:target,approved_headline:input.headline,approved_summary:input.summary};if(target==='published')Object.assign(patch,{public_slug:publicSlug,published_at:new Date().toISOString()});const updated=await supabase.from('experiences').update(patch).eq('id',id);if(updated.error)throw updated.error;const action=await supabase.from('moderation_actions').insert({experience_id:id,moderator_id:profile.id,action:input.action,private_reason:input.privateReason});if(action.error)throw action.error;return NextResponse.json({status:target,publicSlug})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Moderation failed'},{status:400})}}
